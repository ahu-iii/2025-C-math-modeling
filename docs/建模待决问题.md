# 建模待决问题（Open Questions Log）

> 用途：队伍放假、无法实时讨论期间的"异步决策清单"。以下每一条都是 `docs/建模方案.md` 里存在多种合理口径、尚未最终拍板的判断题（不是数学题，靠讨论/共识解决，不是靠算法解决）。
> 使用方法：团队复工后，逐条讨论 → 在"谁需拍板"栏对应的人确认 → 把结论写回 `docs/adr/` 对应的 ADR 文件（状态改为"已接受"，删掉"待定"字样），并同步更新代码常量。
> **在本清单上的条目定案之前，代码里应先用当前默认建议跑通全流程，不要卡在这里等，但下游所有产出物（表/图/论文段落）必须标注"暂用默认口径 X，待确认"，避免定案后忘记回头改。**

---

## 1. 缓解的判据：D_12=0（单点）还是 D_6=D_12=0（连续两点）？

- **当前默认建议**：D_12 = 0（仅看第 12 月）。
- **权衡/理由**：单点定义计算简单、缓解率数值更高、更贴近"疗程结束时是否见效"这个直观问题；两点定义（D_6=D_12=0）更贴近临床"持续缓解"的严谨含义，能排除"第 6 月还有症状、第 12 月刚好不适消失"的偶然波动，但会拉低缓解率、且对第 6 月的数据质量更敏感（万一第 6 月记录缺失多，会连带影响缓解判定）。两种定义在建模方案.md 2.3 节中是并列给出的，未二选一。
- **谁需拍板**：全队（这是问题3 核心指标的地基，建议开会 5 分钟内定，定完写回 ADR-0003）。

## 2. 复发的"曾缓解"判据：D_3=0 或 D_6=0（宽松）还是仅 D_6=0（严格）？

- **当前默认建议**：D_3=0 或 D_6=0 二者满足其一（宽松版）。
- **权衡/理由**：宽松版能捕捉"第 3 月就已经缓解、之后又复发"的个体，样本覆盖更全；严格版（仅看 D_6）更贴近"以标准随访窗口判定缓解状态"的常规做法，逻辑更干净但会丢弃一部分"早期缓解者"。这一条与"缓解定义"（问题1）联动——如果缓解改成两点定义（D_6=D_12=0才算缓解），那么"曾缓解"的判据也需要相应调整，两处必须联动决定，不能分开定。
- **谁需拍板**：全队，且需与问题 1（缓解定义）一起讨论，避免两个 ADR 互相矛盾。

## 3. 复发率的分母：曾缓解人数 vs 全体受试者？

- **当前默认建议**：分母 = 曾缓解人数（即"复发率"是条件概率：在缓解的人里，多少比例又复发了）。
- **权衡/理由**：这是临床文献中"复发率"的标准定义（conditional on remission），符合建模方案.md 原文强调"分母是缓解者，不是全体，务必注意"；缺点是分母人数少、口径解释成本高，需要在论文里反复强调"这里的分母不是总人数"，避免读者/评委误读。可以同时报告一个"全体受试者中复发的比例"作为辅助描述指标，但不作为主指标进入综合排名。
- **谁需拍板**：这条建模方案.md 已经给出较明确的倾向性表述（"分母是缓解者"），实质上接近已定，建议全队快速确认一下即可，无需长时间讨论。

## 4. "失访"是否要以某种方式计入 D_t 或单独建模？

- **当前默认建议**：失访不计入 D_t（既不算有不适，也不算无不适），直接按现有记录计算，在论文局限性中说明。
- **权衡/理由**：题目在问题3 明确"不需要考虑数据缺失和失访对评估的影响"，所以当前默认做法（忽略失访、按现有记录直接算）是符合题目要求的最省事口径；但如果失访比例在三组间分布不均（类似婚姻状况在B组的缺失模式），可能会造成"看似疗效好，其实是因为不良反应者提前失访、没被记录"的偏倚，是否需要额外做一次"失访率按组分布是否均衡"的核查（类似问题1对婚姻状况缺失的处理方式），值得讨论。
- **谁需拍板**：编程者先跑一个"失访按组分布"的描述性统计，如果不均衡，提交给全队讨论是否需要补充说明或敏感性分析。

## 5. 初始抑郁程度在问题2 回归里怎么编码：有序变量（1/2/3）还是哑变量（dummy）？

- **当前默认建议**：有序变量，赋值 轻度=1 / 中度=2 / 重度=3，作为连续型协变量放入回归（或用有序Logistic专门处理）。
- **权衡/理由**：有序编码假设"程度每增加一级，效应大小近似线性/单调"，模型更简洁、系数更好解释（一个OR就能讲完"程度每加重一级，结局风险变化多少"），但如果实际关系不是单调的（比如中度反而比轻度和重度都特殊），有序编码会掩盖这种非线性；哑变量编码（以轻度为参照，中度、重度各一个哑变量）更灵活，能捕捉非单调关系，但需要额外确认参照组、系数数量翻倍、解释成本上升。建模方案.md 原始定义里已注明"有序，可赋 1/2/3"，倾向性较明确，但未排除哑变量作为稳健性检验的可能。
- **谁需拍板**：编程者可先用有序编码出主结果，若时间允许，用哑变量编码做一次稳健性对照，二者结论一致则更有说服力；是否值得花时间做这个对照，由全队根据时间紧张程度决定。

## 6. 症状负担 B_t 的数据来源：逐症状栏重新计数 vs 附件2"是否出现不适症状"栏？

- **当前默认建议**：逐症状栏重新计数（8 类症状栏中标记为1的个数之和）。
- **权衡/理由**：重新计数口径透明、可控，缺点是需要额外一步数据处理；附件2"是否出现不适症状"栏如果本身就是同样的计数汇总，两者应该完全等价，直接用现成栏更省事，但**该栏的实际编码含义尚未核实**（有可能只是二元标记，不是计数），核实前不能贸然采用，以免与逐症状重新计数的结果对不上。
- **谁需拍板**：编程者先写一行代码核对"重新计数"与"现成栏位"在样本上是否完全一致，如果一致，两种写法等价、无需讨论；如果不一致，需要队内确认题目对该栏的原始定义并选定口径。

## 7. TOPSIS 权重来源：纯熵权法（客观）还是熵权+专家权重结合？

- **当前默认建议**：纯熵权法（客观赋权，权重由各指标在三组间的区分度决定）。
- **权衡/理由**：熵权法完全数据驱动、可复现、不依赖个人判断，符合"客观赋权"的题目导向；缺点是如果某个临床上很重要的指标（比如"有自杀倾向发生率"这种安全性红线指标）恰好在数据里区分度很小，熵权法会给它很低的权重，这在临床上可能不合理——不良反应的安全性权重也许应该有一个"下限"或由专家判断上调，而不是完全交给数据。建模方案.md 中把"专家/文献赋权"列为熵权法的备选项，未强制排除。
- **谁需拍板**：非编程队友（更适合评估"临床上安全性指标该占多大权重"这类问题），建议至少对"自杀倾向发生率"这一项做一次单独讨论，决定是否需要在纯熵权结果之上做人工修正，并在论文里写明修正理由。

## 8. 婚姻状况"未知"类别（集中在B组的70人）在问题2 回归里怎么处理？

- **当前默认建议**：保留为独立的"未知"类别参与回归（与问题1 一致的处理方式），不删除样本。
- **权衡/理由**：保留"未知"类别不损失样本量，且问题1 已证明该缺失非随机（集中在B组）——如果问题2 直接删除这些人，等于人为改变B组（药物B）的样本构成，可能引入选择偏倚，扭曲"药物B疗效"的估计。缺点是"未知"类别本身没有实际信息，其回归系数不好解释，且如果B组"未知"人数较多，可能拉低该类别系数的估计精度。可选的稳健性检验：单独排除"未知"样本重新跑一次婚姻状况这一项，对比结论是否一致。
- **谁需拍板**：编程者按默认口径先出结果；是否需要额外做"排除未知样本"的稳健性对照，由全队根据时间决定（问题1_实现说明.md 第七节已经提醒了这一点，说明队内已经意识到需要提前商量）。

## 9. 适应期豁免规则要不要影响症状负担 B_t 的数值，还是只影响"是否计入药物质量问题"的判定？

- **当前默认建议**：只影响不良反应发生率等"是否计入药物质量问题"类指标的判定，不改动 B_t 原始数值。
- **权衡/理由**：不改动 B_t 保持指标定义单纯、便于交叉核对（B_t 就是"当时观测到的症状数"，不掺入判断逻辑）；但这样"症状改善程度 ΔB=B_1-B_12"这个指标里，适应期个体前期的不适仍然会被计入 B_1，可能拉高 ΔB（看起来"改善"更多），这本身其实和适应期规则想表达的意思是一致的（适应期个体确实是"前期有事、后期没事"），所以两种指标（是否计入质量问题 vs B_t数值）目前看是不冲突的，但需要队内确认这个理解是否一致，避免论文里两处表述打架。
- **谁需拍板**：全队快速确认一下逻辑是否自洽即可，预计不需要长时间讨论。

## 10. 问题2 是否需要补充一次正式的"因素效应是否因药物而异"的检验？

- **当前默认建议**：不补充，仅做三组分层模型系数的描述性对比（见 ADR-0006）。
- **权衡/理由**：题目约束下无法在同一模型里放入"药物×因素"交互项（组别与药物完全共线），分层模型是唯一可行的主分析方式；但分层模型只能"描述性地"说"A组里这个因素的OR是2.1，B组是1.3，看起来不太一样"，无法给出一个正式的p值来说"这个不一样在统计上是否显著"。如果评委/论文需要更严谨的表述，可以考虑用其他方式间接检验（比如把三组数据汇总，去掉与组别共线的"药物主效应"，只放"因素×组别"交互项，专门检验交互项是否显著），但这会增加工作量。
- **谁需拍板**：全队评估这个"锦上添花"的检验值得不值得做，若时间紧张可以直接跳过、只做描述性对比，并在论文局限性里说明。

---

## 使用提醒
- 以上 10 条中，第 1、2、3 三条互相关联（缓解定义 ↔ 复发判据 ↔ 复发分母），建议一次性讨论完，不要分批定，否则容易出现口径不自洽。
- 每条定案后，回到 `docs/adr/` 对应文件，把"状态"从"待定/已接受（XX待确认）"改成"已接受"，并删除本文件里对应的条目（或标注"已定案，见 ADR-000X"）。
- 本清单不是最终清单——如果编程/写作过程中又发现新的模糊点，请随时按同样的格式（问题/当前默认建议/权衡理由/谁需拍板）补充在本文件末尾，保持它是"当前唯一权威的待办列表"。
